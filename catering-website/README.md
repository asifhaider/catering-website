# Homestyle Catering

A homemade-food catering ordering site (React + TypeScript + Vite + Tailwind CSS).

## Development

```
npm install
npm run dev
```

## Build

```
npm run build
```

Menu data is dummy/placeholder content in `src/data/menuData.ts`. Orders are stored client-side only (localStorage); there is no backend or real payment processing.
